# 📚 Шпаргалка по SCSS сниппетам

> Быстрые сниппеты для работы с системой дизайна проекта

---

## 🚀 Как использовать

1. Начните печатать **префикс** (например, `sp`)
2. Нажмите **Tab** для вставки
3. Используйте **Tab** для перехода между полями
4. В выпадающих списках используйте **стрелки** для выбора

---

## 📦 Импорты

| Префикс | Результат                                                                                                            | Описание                        |
| ------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| `imp`   | `@use '../abstracts/variables' as *;`<br>`@use '../abstracts/functions' as *;`<br>`@use '../abstracts/mixins' as *;` | Импорт всех abstracts модулей   |
| `impc`  | То же + `component-mixins`                                                                                           | Импорт включая component-mixins |

**Пример:**

```scss
imp [Tab]
// → Вставит все импорты
```

---

## 🎨 Функции

### Основные функции

| Префикс | Результат         | Описание             |
| ------- | ----------------- | -------------------- |
| `sp`    | `spacing(4)`      | Отступы из системы   |
| `px`    | `px2rem(16)`      | Конвертация px → rem |
| `rad`   | `radius('md')`    | Радиус скругления    |
| `shad`  | `shadow('md')`    | Тень                 |
| `zi`    | `z-index('base')` | Z-index из системы   |

### Цвета

| Префикс   | Результат            | Описание        |
| --------- | -------------------- | --------------- |
| `col`     | `color('primary')`   | Цвет из палитры |
| `colgray` | `color('gray', 500)` | Серый оттенок   |

**Примеры:**

```scss
// Отступы
padding: sp [Tab] → padding: spacing(4);

// Конвертация
width: px [Tab] → width: px2rem(16);

// Цвета
background: col [Tab] → background: color('primary');
border: 1px solid colgray [Tab] → border: 1px solid color('gray', 300);
```

---

## 📱 Адаптивность

| Префикс | Результат                          | Описание              |
| ------- | ---------------------------------- | --------------------- |
| `rmax`  | `@include respond-max(tablet) { }` | Max-width media query |
| `rmin`  | `@include respond-to(md) { }`      | Min-width media query |

**Доступные breakpoints:**

- `phone` (767px)
- `tablet` (1240px)
- `md` (768px)
- `lg` (992px)
- `xl` (1200px)

**Примеры:**

```scss
.element {
  padding: spacing(8);

  rmax [Tab] → выберите tablet
  // @include respond-max(tablet) {
  //   padding: spacing(4);
  // }
}
```

---

## 📐 Grid миксины

| Префикс    | Результат                                        | Описание                       |
| ---------- | ------------------------------------------------ | ------------------------------ |
| `gautof`   | `@include grid-auto-fit(280px, spacing(6));`     | Адаптивная сетка (auto-fit) ⭐ |
| `gresp`    | `@include grid-responsive(1, 2, 3, spacing(6));` | Сетка с breakpoints            |
| `gsidebar` | `@include grid-sidebar(280px, spacing(8));`      | Layout с боковой панелью       |
| `gspan`    | `@include grid-span(2, 1);`                      | Растяжение элемента            |
| `gcenter`  | `@include grid-center;`                          | Центрирование                  |
| `gcards`   | `@include grid-cards(280px, 4, spacing(6));`     | Готовая сетка карточек         |

**Примеры:**

```scss
// Популярный паттерн: адаптивная сетка карточек
.products {
  gautof [Tab]
  // → @include grid-auto-fit(280px, spacing(6));
}

// Layout с сайдбаром
.page {
  gsidebar [Tab]
  // → @include grid-sidebar(280px, spacing(8));
}

// Выделенная карточка (занимает 2 места)
.featured {
  gspan [Tab]
  // → @include grid-span(2, 1);
}
```

---

## 💪 Flexbox миксины

| Префикс    | Результат                | Описание                     |
| ---------- | ------------------------ | ---------------------------- |
| `fcenter`  | `@include flex-center;`  | Центрирование по обеим осям  |
| `fbetween` | `@include flex-between;` | Space-between с align center |
| `fcolumn`  | `@include flex-column;`  | Flex направление колонкой    |

**Примеры:**

```scss
.modal {
  fcenter [Tab]
  // → @include flex-center;
}

.header {
  fbetween [Tab]
  // → @include flex-between;
}
```

---

## 🎯 Типографика

| Префикс | Результат                | Описание                |
| ------- | ------------------------ | ----------------------- |
| `type`  | `@include type(16, 24);` | Font-size + line-height |
| `crop`  | `@include cropText(3);`  | Ограничение строк       |

**Примеры:**

```scss
h1 {
  type [Tab]
  // → @include type(48, 56);
}

.description {
  crop [Tab]
  // → @include cropText(3); // Макс 3 строки
}
```

---

## 🔘 Компонент-миксины

| Префикс  | Результат                                     | Описание           |
| -------- | --------------------------------------------- | ------------------ |
| `mbtn`   | `@include btn(var(--primary), var(--white));` | Готовая кнопка     |
| `minput` | `@include input(64px);`                       | Готовое поле ввода |

**Примеры:**

```scss
.button {
  mbtn [Tab]
  // → @include btn(var(--primary), var(--white));
}

.form-input {
  minput [Tab]
  // → @include input(64px);
}
```

---

## ⚡ Быстрые стили

### Отступы

| Префикс | Результат                         | Описание          |
| ------- | --------------------------------- | ----------------- |
| `pad`   | `padding: spacing(4);`            | Padding           |
| `mar`   | `margin: spacing(4);`             | Margin            |
| `padxy` | `padding: spacing(4) spacing(6);` | Padding по осям   |
| `gap`   | `gap: spacing(4);`                | Gap для grid/flex |

### Цвета и границы

| Префикс | Результат                               | Описание   |
| ------- | --------------------------------------- | ---------- |
| `bgc`   | `background: color('primary');`         | Background |
| `bord`  | `border: 1px solid color('gray', 300);` | Border     |

**Примеры:**

```scss
.card {
  pad [Tab]      → padding: spacing(6);
  mar [Tab]      → margin: spacing(4);
  bgc [Tab]      → background: color('white');
  bord [Tab]     → border: 1px solid color('gray', 300);
}
```

---

## 🏗️ БЭМ структуры

| Префикс | Результат            | Описание                     |
| ------- | -------------------- | ---------------------------- |
| `bem`   | Полная БЭМ структура | Блок + элемент + модификатор |
| `beme`  | `&__element { }`     | Элемент                      |
| `bemm`  | `&--modifier { }`    | Модификатор                  |

**Примеры:**

```scss
// Создать полную БЭМ структуру
bem [Tab]
// .block {
//   &__element { }
//   &--modifier { }
// }

// Добавить элемент
beme [Tab]
// &__element { }

// Добавить модификатор
bemm [Tab]
// &--modifier { }
```

---

## 🎁 Готовые паттерны

### Card (Карточка)

**Префикс:** `card`

```scss
card [Tab]
// .card {
//   background: color('white');
//   padding: spacing(6);
//   border-radius: radius('lg');
//   box-shadow: shadow('md');
//   transition: all transition('base') easing('out');
//
//   &:hover {
//     box-shadow: shadow('lg');
//     transform: translateY(px2rem(-4));
//   }
// }
```

### Hover Scale (Масштабирование при наведении)

**Префикс:** `hscale`

```scss
hscale [Tab]
// transition: transform transition('base') easing('out');
//
// &:hover {
//   transform: scale(1.05);
// }
```

### Adaptive Padding (Адаптивные отступы)

**Префикс:** `apad`

```scss
apad [Tab]
// padding: spacing(8) spacing(6);
//
// @include respond-max(tablet) {
//   padding: spacing(6) spacing(4);
// }
```

---

## 🎓 Частые комбинации

### Адаптивная сетка товаров

```scss
.products {
  gautof [Tab] → @include grid-auto-fit(280px, spacing(6));

  rmax [Tab] → tablet
  // @include respond-max(tablet) {
  //   gap: spacing(4);
  // }
}
```

### Карточка с hover эффектом

```scss
card [Tab]
// Создаст готовую карточку со всеми стилями
```

### Модальное окно

```scss
.modal {
  position: fixed;
  fcenter [Tab]     → @include flex-center;
  zi [Tab] → modal  → z-index: z-index('modal');

  &__content {
    pad [Tab]       → padding: spacing(8);
    bgc [Tab]       → background: color('white');
    rad [Tab] → lg  → border-radius: radius('lg');
    shad [Tab] → xl → box-shadow: shadow('xl');
  }
}
```

### Адаптивная типографика

```scss
h1 {
  type [Tab] → 48, 56
  // @include type(48, 56);

  rmax [Tab] → phone
    type [Tab] → 32, 40
  // @include respond-max(phone) {
  //   @include type(32, 40);
  // }
}
```

---

## 📊 Шпаргалка по значениям

### Spacing (отступы)

- `0` → 0
- `1` → 4px
- `2` → 8px
- `3` → 12px
- `4` → 16px ⭐
- `5` → 20px
- `6` → 24px ⭐
- `8` → 32px
- `10` → 40px
- `12` → 48px
- `16` → 64px
- `20` → 80px

### Colors (цвета)

**Основные:**

- `primary` → #42b883
- `secondary` → #35495e
- `success` → #28a745
- `danger` → #dc3545
- `warning` → #ffc107
- `white` → #ffffff
- `black` → #000000

**Gray (оттенки):**

- `100` → #f8f9fa (почти белый)
- `300` → #dee2e6 (светлые границы)
- `500` → #adb5bd (средний)
- `600` → #6c757d (второстепенный текст)
- `700` → #495057 (основной текст)
- `900` → #212529 (заголовки)

### Radius (скругления)

- `sm` → 2px
- `base` → 4px
- `md` → 6px ⭐
- `lg` → 8px ⭐
- `xl` → 12px
- `2xl` → 16px
- `full` → 9999px (круг)

### Shadow (тени)

- `sm` → легкая
- `base` → базовая
- `md` → средняя ⭐
- `lg` → большая
- `xl` → очень большая

### Z-index (слои)

**Локальные:**

- `behind` → -1
- `base` → 0
- `above` → 1 ⭐
- `overlay` → 10

**Глобальные:**

- `dropdown` → 1000
- `sticky` → 1020
- `fixed` → 1030
- `modal` → 1050
- `tooltip` → 1070

---

## 💡 Советы по использованию

1. **Всегда начинайте с `imp`** — импортирует все нужные модули
2. **Используйте `gautof`** — самый популярный паттерн для сеток
3. **`rmax` + `tablet`** — частая комбинация для адаптива
4. **`card`** — быстро создает красивую карточку
5. **`sp`, `col`, `rad`** — используйте вместо хардкода значений
6. **`type`** — вместо отдельных font-size и line-height

---

## 🔥 Топ-5 самых используемых

1. **`gautof`** — адаптивная сетка (grid-auto-fit)
2. **`rmax`** — адаптивность (respond-max)
3. **`sp`** — отступы (spacing)
4. **`col`** — цвета (color)
5. **`type`** — типографика (type mixin)

---

**Создано:** 18 февраля 2026 г.
**Версия:** 1.0

> 💡 **Совет:** Распечатайте эту шпаргалку или держите открытой во время работы!
